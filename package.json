{
  "name": "israfel",
  "version": "1.0.0",
  "private": true,
  "description": "israfel",
  "author": "FinBook Pte Ltd",
  "scripts": {
    "test": "truffle.cmd test",
    "clean": "rimraf build",
    "start": "ganache-cli",
    "build": "truffle.cmd migrate",
    "rebuild": "npm run clean && truffle.cmd migrate --reset --compile-all"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-plugin-mocha": "^5.0.0",
    "ganache-cli": "^6.1.0",
    "rimraf": "^2.6.2",
    "truffle": "^4.1.0"
  },
  "dependencies": {
    "web3": "^1.0.0-beta.33"
  }
}
