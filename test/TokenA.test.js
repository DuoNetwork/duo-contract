let TokenA = artifacts.require('./TokenA.sol');

// contract('TokenA', () => {
// 	it('should be deployed', () => {
// 		return TokenA.deployed().then(instance => assert.isTrue(!!instance));
// 	});

	// it('total supply should be 0', () => {
	// 	return TokenA.deployed().then(instance => {
	// 		instance.totalSupply
	// 			.call()
	// 			.then(totalSupply =>
	// 				assert.equal(
	// 					totalSupply.valueOf(),
	// 					0,
	// 					'totalSupply not equal to 0'
	// 				)
	// 			);
	// 	});
	// });

	// it('should show balance', () => {
	// 	return TokenA.deployed().then(instance => assert.isTrue(!instance));
	// });

	// it('should show allowance', () => {
	// 	return TokenA.deployed().then(instance => assert.isTrue(!instance));
	// });

	// it('should be able to transfer', () => {
	// 	return TokenA.deployed().then(instance => assert.isTrue(!instance));
	// });

	// it('should not transfer more than balance', () => {
	// 	return TokenA.deployed().then(instance => assert.isTrue(!instance));
	// });

	// it('should be able to approve', () => {
	// 	return TokenA.deployed().then(instance => assert.isTrue(!instance));
	// });
// });
